/**
 * Represents a team member in the company profile section.
 */
export interface TeamMember {
  readonly name: string;
  readonly role: string;
  readonly image: string;
  readonly desc: string;
}
