/**
 * Represents a service offering displayed in the business plan.
 */
export interface ServiceItem {
  readonly name: string;
  readonly tag: string;
  readonly desc: string;
  readonly color: string;
  readonly textColor: string;
}
